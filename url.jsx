const devMode = {
    mode : 'Development mode',
    clientUrl : "http://localhost:3000",
    serverUrl : "http://localhost:5000"
}

const prodMode = {
    mode : 'Production Mode'
}

const CurrentMode = devMode


export default CurrentMode;